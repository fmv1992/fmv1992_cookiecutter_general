project Project_{{ cookiecutter.__project_slug.split('_') | map('capitalize') | join('_') }} is
    for Main use ("main.adb");
    for Source_Dirs use ("src");
    for Object_Dir use "obj";
    for Exec_Dir use "bin";
    package Compiler is
        for Switches ("Ada") use ("-gnat2022");
    end Compiler;
end Project_{{ cookiecutter.__project_slug.split('_') | map('capitalize') | join('_') }};

-- vim: set filetype=ada fileformat=unix nowrap spell spelllang=en,cdenglish01:
